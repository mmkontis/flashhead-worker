opencv-python-headless>=4.12.0.88
diffusers>=0.34.0
transformers==4.57.3
tokenizers>=0.20.3
accelerate>=1.8.1
tqdm
imageio
easydict
ftfy
imageio-ffmpeg
scikit-image
loguru
xfuser>=0.4.3
pyloudnorm
decord
xformers==0.0.31
librosa
# mediapipe removed: pins protobuf<4 which conflicts with livekit (protobuf>=5).
# Only needed for use_face_crop=True; the import is patched to be optional in the Dockerfile.
runpod

# LiveKit streaming avatar mode (worker joins the room, lk_avatar.py)
livekit-agents~=1.2.18
